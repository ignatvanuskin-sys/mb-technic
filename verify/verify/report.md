# VERIFY — MB Technic (http://localhost:3000/?v=fix1)

Метод: Chrome + CDP device-metrics override (эмуляция), не ресайз окна. Все замеры — на живой странице.
Файлы: `verify/*.png`, скрипт замеров `verify/measure_a45.js`.

## A. Пять фиксов

### A1. Футер модалки виден без скролла — PASS
Кнопка «Отправить заявку» (шаг 5 «Контакты»), `getBoundingClientRect().bottom` vs `window.innerHeight`:

| Вьюпорт | innerHeight | submit.top | submit.bottom | height | bottom < innerHeight |
|---|---|---|---|---|---|
| 390x844 | 844 | 785 | **827** | 42 | **ДА (PASS)** |
| 320x568 | 568 | 492 | **552** | 60 | **ДА (PASS)** |

Футер прижат к низу, скролл не нужен. Скриншоты: `m390-booking-step5.png`, `m320-booking-step5.png`.

### A2. Внутренний скролл модалки — PASS
Примечание: `document.querySelector('[role="dialog"]')` возвращает **первым** drawer меню (высота 0 при закрытом).
Модалка записи — **второй** `[role="dialog"]` (overflow hidden, h=844). Замеры по ней:

| Шаг | dialog.scrollTop | dialog sh/ch | Внутр. скроллер sh/ch |
|---|---|---|---|
| Шаг 2 «Ваш Mercedes» @390x844 | 0 | 844 / 844 (не скроллится) | **678 / 666** (скроллится) |
| Шаг 5 «Контакты» @390x844 | 0 | 844 / 844 | нет скроллера (контент помещается) |
| Шаг 5 «Контакты» @320x568 | 0 | 568 / 568 | **664 / 374** (скроллится) |

Диалог сам не прокручивается (scrollTop=0), скроллится внутренний step-body. PASS.

### A3. Поведение мобильного меню @390x844 — PASS (все)
| Проверка | Результат | Доказательство |
|---|---|---|
| Закрытие по **Escape** | **true** | после Escape `[role=dialog]` drawer исчез (drawerStillOpen=false) |
| Закрытие по тапу на **фон** | **true** | `elementFromPoint(195,810)` = div `fixed inset-0 z-[94] bg-ink/70` (не диалог) → drawer закрылся |
| НЕ full-bleed | **true** | rect top=76, bottom=776, **h=700** vs innerHeight 844 → полоса фона **68px** снизу видна |
| Фокус внутрь при открытии | **true** | `document.activeElement` = A «Услуги» (текст «Услуги01»), внутри drawer |
| Фокус возвращается на гамбургер | **true** | после закрытия activeElement aria-label = **«Открыть меню»** |
| Tab-trap | **true** | 10×Tab: все 10 focusin внутри drawer, цикл Услуги→О компании→…→+7 700→Услуги; `everLeft=false` |
| Блокировка скролла фона | **true** | scrollY 300 → попытка скролла → **300** (locked); body overflow:hidden |
| Атрибуты drawer | role=`dialog`, aria-modal=`true`, aria-label=`Меню` | — |

Скриншот: `m390-menu-open.png`.

### A4. Порог типографики — PASS
- Текстовых узлов с font-size < 12px: **61** (было 92).
- Минимальный размер: **11px** → наименьший >= 11px: **ДА (PASS)**.

Примеры (текст / размер):
`MB TECHNIC` 11 · `/` 11 · `Mercedes-Benz` 11 · `Астана` 11 · `5.0` 11.0 · `37` 11.0 · `ОЦЕНОК В` 11.0 · `2ГИС` 11.0 · `ЕЖЕДНЕВНО` 11.0 · `09:00 — 19:00` 11.0 (класс `hero-*-label / hero-trust-hours`, `text-[0.688rem]`).

### A5. Touch-таргеты < 44x44 @390x844 — 10 шт (было 36)
| # | Селектор/класс | WxH | Текст | Тип |
|---|---|---|---|---|
| 1 | a.sr-only (skip-link) | 1x1 | «Перейти к содержимому» | служебный (скрыт) |
| 2 | a `text-white/55 underline` | 30x15 | «2ГИС» | плотная ссылка |
| 3 | a `text-white` | 303x26 | «+7 700 706 22 20» | контакт-ссылка |
| 4 | a `text-white` | 303x26 | «+7 771 149 24 99» | контакт-ссылка |
| 5 | a `text-white` | 197x26 | «WhatsApp +7 700 706 22 20» | контакт-ссылка |
| 6 | a `text-white` | 192x26 | «WhatsApp +7 771 149 24 99» | контакт-ссылка |
| 7 | a `text-white` | 85x26 | «Онлайн-чат» | контакт-ссылка |
| 8 | a `absolute bottom-4 right-4 border` | 165x40 | «ОТКРЫТЬ В 2ГИС» | вторичная кнопка на карте |
| 9 | a `font-mono text-[0.75rem]` | 246x18 | «ПОЛИТИКА КОНФИДЕНЦИАЛЬНОСТИ» | плотная футер-ссылка |
| 10 | a `font-mono text-[0.75rem]` | 119x18 | «ПАНЕЛЬ ЗАЯВОК» | плотная футер-ссылка |

Основных действий (CTA) среди мелких нет: PAST — все 10 это плотные футер/контакт-ссылки и служебный skip-link. Основные CTA (ЗАПИСАТЬСЯ / ПОЗВОНИТЬ / направление услуг) >= 44px.
